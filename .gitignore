# ── 统一本地前缀 ──────────────────────────────────────────────
# 本地专属、不入库的东西统一用这个约定:放进 .local/ 目录,或给文件加 .local 后缀。
# (部署脚本、私钥、服务器快照、私人分析草稿、临时文件……一条规则全覆盖)
.local/
*.local
*.local.*

# 凭证 / 私钥 —— 安全网,纵深防御(即使放错位置也绝不入库)
.mcp.json
*.pem
*.key
.env
.env.*
!.env.example

# Node
node_modules/
dist/
*.log

# PageFire local data
dev-data/

# OS
.DS_Store
Thumbs.db
.playwright-mcp/

# 本地截图 / 设计草稿（开发过程中产生，不属于仓库内容）
*.png
*.jpg
*.jpeg
!docs/**/*.png
!docs/**/*.jpg

# 私人分析文档（不入库）
*_Skill_v*.md
估值预期分析*.md

# 部署文档 —— 含服务器信息，仅保留本地，不入库
docs/deploy/

# 开发工具内部配置（speckit / Claude Code）
.specify/
.claude/
