# ── 统一本地前缀 ──────────────────────────────────────────────
# 本地专属、不入库的东西统一用这些约定:放进 .local/ / tmp/ / temp/ / private/ / scratch/ 目录,
# 或给文件加 .local / .tmp / .private 后缀。一条规则全覆盖,省得为每种文件名写脆弱 glob。
# 注意:用"目录名 + 后缀"而非子串匹配,避免误伤 privateKey.ts 这类该提交的源码。
.local/
tmp/
temp/
private/
scratch/
*.local
*.local.*
*.tmp
*.private
*.private.*

# 凭证 / 私钥 —— 安全网,纵深防御(即使放错位置也绝不入库)
.mcp.json
*.pem
*.key
.env
.env.*
!.env.example

# Node
node_modules/
dist/
*.log

# PageFire local data
dev-data/

# OS
.DS_Store
Thumbs.db
.playwright-mcp/

# 本地截图 / 设计草稿（开发过程中产生，不属于仓库内容）
*.png
*.jpg
*.jpeg
!docs/**/*.png
!docs/**/*.jpg

# 部署文档 / 服务器相关 —— 含敏感信息，仅本地保留(目录约定,无需写具体文件名)
docs/deploy/

# 开发工具内部配置（speckit / Claude Code）
.specify/
.claude/
