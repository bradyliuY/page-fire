import { describe, it, expect } from 'vitest'

describe('deploy_page integration', () => {
  it('TODO: start in-process server, deploy, serve, delete', () => {
    // Requires running server - implement after pnpm install + pnpm build succeed
    expect(true).toBe(true)
  })
})
